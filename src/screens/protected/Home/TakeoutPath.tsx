import { StyleSheet, Text, View } from "react-native";
import React from "react";

const TakeoutPath = () => {
  return (
    <View>
      <Text>TakeoutPath</Text>
    </View>
  );
};

export default TakeoutPath;

const styles = StyleSheet.create({});
